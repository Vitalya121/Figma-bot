[phases.setup]
nixPkgs = ["nodejs_22", "pnpm"]

[phases.install]
cmds = ["pnpm install"]

[phases.build]
cmds = [
  "pnpm --filter @carousel-forge/types build",
  "pnpm --filter @carousel-forge/config build",
]
